pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "Building in dev branch"
            }
        }
        stage('Test') {
            steps {
                echo "testing the dev branch"
            }
        }
        stage('Deploy') {
            steps {
                echo "deploying of the dev branch"
            }
        }
    }
}
